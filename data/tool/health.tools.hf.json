[
  {
    "type": "function",
    "function": {
      "name": "health_read_medical_records",
      "description": "Read person records from the medical database for a specified date.",
      "parameters": {
        "type": "object",
        "properties": {
          "person": {
            "type": "string",
            "description": "human-readable person name, unique per medical database."
          },
          "date": {
            "type": "string",
            "description": "the past date we want to retrieve the medical records, in ISO 8601 format, e.g. 2026-03-24"
          }
        },
        "required": [
          "person",
          "date"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "health_save_blood_measurement",
      "description": "Save person blood pressure and cardiac pulse in the medical database. Also compute pulse pressure as difference between systolic and diastolic pressures.",
      "parameters": {
        "type": "object",
        "properties": {
          "person": {
            "type": "string",
            "description": "human-readable person name, unique per medical database."
          },
          "systole": {
            "type": "integer",
            "description": "systolic pressure expressed as mmHg (millimeters of mercury)."
          },
          "diastole": {
            "type": "integer",
            "description": "diastolic pressure expressed as mmHg (millimeters of mercury)."
          },
          "pulse": {
            "type": "integer",
            "description": "heart rate expressed in bpm (beats per minute)."
          }
        },
        "required": [
          "person",
          "systole",
          "diastole",
          "pulse"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "health_save_glucose",
      "description": "Save person blood glucose level in the medical database.",
      "parameters": {
        "type": "object",
        "properties": {
          "person": {
            "type": "string",
            "description": "human-readable person name, unique per medical database."
          },
          "glucose": {
            "type": "integer",
            "description": "blood glucose level expressed in mg/dL (milligrams per deciliter)."
          }
        },
        "required": [
          "person",
          "glucose"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "health_save_temperature",
      "description": "Save person body temperature in the medical database.",
      "parameters": {
        "type": "object",
        "properties": {
          "person": {
            "type": "string",
            "description": "human-readable person name, unique per medical database."
          },
          "temperature": {
            "type": "number",
            "description": "body temperature expressed in °C (Celsius degrees)"
          }
        },
        "required": [
          "person",
          "temperature"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "health_save_weight",
      "description": "Save person body weight in the medical database. Also compute body mass index based on person height.",
      "parameters": {
        "type": "object",
        "properties": {
          "person": {
            "type": "string",
            "description": "human-readable person name, unique per medical database."
          },
          "weight": {
            "type": "number",
            "description": "body weight expressed in kg (kilogram)."
          },
          "height": {
            "type": "integer",
            "description": "person height expressed in cm (centimeters)."
          }
        },
        "required": [
          "person",
          "weight",
          "height"
        ]
      }
    }
  }
]