[
  {
    "type": "function",
    "function": {
      "name": "hera_list_devices",
      "description": "List all devices registered to home automation server",
      "parameters": {
        "type": "OBJECT",
        "properties": {}
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "hera_describe_device",
      "description": "Create a description for the requested device, including supported actions list",
      "parameters": {
        "type": "OBJECT",
        "properties": {
          "device": {
            "type": "STRING",
            "description": "The unique ID or name of the device to be described"
          }
        },
        "required": [
          "device"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "hera_read_temperature",
      "description": "Reads the current temperature value for the requested zone",
      "parameters": {
        "type": "OBJECT",
        "properties": {
          "zone": {
            "type": "STRING",
            "description": "Name of the zone for which the temperature value is checked"
          }
        },
        "required": [
          "zone"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "hera_read_humidity",
      "description": "Reads the current humidity value for the requested zone",
      "parameters": {
        "type": "OBJECT",
        "properties": {
          "zone": {
            "type": "STRING",
            "description": "Name of the zone for which the humidity value is checked"
          }
        },
        "required": [
          "zone"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "hera_read_sensors",
      "description": "Get readings from all sensors in the requested zone or the entire system if the zone is not specified",
      "parameters": {
        "type": "OBJECT",
        "properties": {
          "zone": {
            "type": "STRING",
            "description": "The name of the zone from which the sensors data will be retrieved; if the user request does not provide the zone, 'system' is used"
          }
        },
        "required": [
          "zone"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "hera_start_heating",
      "description": "Activates the central heating system to begin warming the home",
      "parameters": {
        "type": "OBJECT",
        "properties": {}
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "hera_stop_heating",
      "description": "Completely disables the central heating system, halting all heating operations",
      "parameters": {
        "type": "OBJECT",
        "properties": {}
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "hera_get_heating_state",
      "description": "Retrieves the current state of the central heating system",
      "parameters": {
        "type": "OBJECT",
        "properties": {}
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "hera_run_diagnose",
      "description": "Verify that the requested device is online and working properly, and create a diagnostic report; diagnose the entire system if a device is not provided by the user request",
      "parameters": {
        "type": "OBJECT",
        "properties": {
          "device": {
            "type": "STRING",
            "description": "The unique ID or name of the device to be diagnosed; use 'system' if the device is not provided by the user request"
          }
        },
        "required": [
          "device"
        ]
      }
    }
  }
]